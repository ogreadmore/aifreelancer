{
  "name": "aifreelancer-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:css": "tailwindcss -c tailwind.config.js -i assets/tailwind.css -o assets/site.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.10"
  }
}

